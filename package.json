{
  "name": "salkaro-portal-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:portal": "turbo dev --filter=@salkaro/portal",
    "dev:web": "turbo dev --filter=@salkaro/web",
    "build:portal": "turbo build --filter=@salkaro/portal",
    "build:web": "turbo build --filter=@salkaro/web"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  },
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": ">=18.12.0",
    "pnpm": ">=10.0.0"
  }
}
